# HTTP
[x] Given a user wants to login, when an HTTP request is incoming with email and password, then invoke the authentication module.
> **Status: COMPLETED**
[x] Given a user wants to login, when an HTTP request is incoming with email and password and credentials are invalid, then return 401.
> **Status: COMPLETED**
[x] Given a user wants to login, when an HTTP request is incoming with email and password and credentials are valid, then return 200 with a JWT token in the response.
> **Status: COMPLETED**
